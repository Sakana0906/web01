//function calculateArea(width: number, height: number): number {
//    return width * height;
//}
//console.log(`The area is: ${calculateArea(8, 10)}`);

const calculateArea = (width: number, height: number): number => {
    return width * height;
}
console.log(`Width: 8  Height: 10\nThe area is: ${calculateArea(8, 10)}`);
